apiVersion: v1
kind: Service

metadata:
  name: dept-service
  namespace: department-dev

spec:
  selector:
    app: dept-service

  ports:
    - port: 80
      targetPort: 8082
      nodePort: 30082

  type: NodePort